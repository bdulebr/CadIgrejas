# LOG DE DESENVOLVIMENTO - 19/06/2026 10:30

## Módulo: PV Drive & Google Workspace (Sincronização Avançada)

### Resumo das Implementações (Goal: Hierarquia GDrive):
1. **Estrutura Mestra de Organização no Google Drive:**
   - Criamos duas pastas matrizes ("Usuarios" e "Departamentos") dentro do PV Drive Raiz (`1ZulCvedeEykQN-Vkj0B2i3o-MLqrZsd1`).
   - Os IDs dessas matrizes foram extraídos via API e gravados dinamicamente no arquivo `.env` local (`GDRIVE_USUARIOS_FOLDER_ID` e `GDRIVE_DEPARTAMENTOS_FOLDER_ID`). Elas foram injetadas também no `intranet/settings.py` para acesso global pelo Django.

2. **Backend (Automagical Signals):**
   - Alteramos os Signals responsáveis pelos cadastros novos de `Membro` e `Departamento` (`core/signals.py`).
   - Agora, quando um usuário aceita o convite/se autocadastra ou o admin cria um Departamento, o sistema NÃO APENAS salva a `PastaVirtual` no banco local, mas **abre uma conexão síncrona com o Google Drive**, cria uma pasta exclusiva para ele dentro da Mestra correspondente, resgata o ID e vincula no banco de dados.
   - A subpasta "Compartilhados Comigo" também é gerada hierarquicamente dentro da pasta pai do usuário.

3. **Sincronização Retroativa:**
   - Para não deixar a base de dados legada na mão, construímos o script `scratch/sync_gdrive_hierarchy.py`. Ele varreu todos os 21 membros e 6 departamentos já existentes no sistema e gerou mais de 50 pastas e subpastas organizadas dentro do Drive Compartilhado, gravando os IDs silenciosamente.

4. **Isolamento de Backups (Sysadmin):**
   - Foi validado que o módulo SysAdmin (através de `core/management/commands/backup_db.py` e `intranet/services/google_drive.py`) continua funcionando como esperado. Os backups de banco de dados (`.zip`) utilizam o ID raiz nativo (`GDRIVE_FOLDER_ID`) e portanto ficarão armazenados no topo do Drive Compartilhado "INTRANET", separados da pasta restrita do "PV Drive".
