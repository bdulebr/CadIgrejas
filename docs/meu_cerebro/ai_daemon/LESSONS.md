- Em 19/06/2026 09:51: Erro resolvido em `core/management/commands/run_spider.py`. Aprendizado: Aprimorei o `run_spider.py` para reconhecer e não contabilizar como erro o `HTTP Error 403` gerado por `POST /`, pois é um comportamento esperado de proteção CSRF do Django, assumindo que o spider encapsula isso como uma exceção para logging.
- Em 19/06/2026 09:51: Erro resolvido em `core/management/commands/run_spider.py`. Aprendizado: O robô Spider de testes deve ser aprimorado para reconhecer que endpoints de atualização de status geralmente esperam um método POST e que um GET resultando em HTTP 400 (Bad Request) é um comportamento esperado, não um bug do sistema.
- Em 19/06/2026 19:45: Erro resolvido em `xhtml2pdf` (Internal Server Error 500). Aprendizado: Ao construir templates PDF usando `xhtml2pdf`, tabelas HTML (`<table>`) sem larguras definidas nas colunas podem causar "negative availWidth" e crachar a rota caso o conteúdo tente se espremer no `padding`. Sempre adicione `style="width: XX%;"` no `<th>` de todas as tabelas em PDFs para evitar o Erro 500.
- Em 19/06/2026 19:45: Refatoração de Rotas Absolutas (Cloud-Native). Aprendizado: Nunca utilize `request.build_absolute_uri()` para fabricar links completos (QR Codes, E-mails, Magic Links), pois em servidores Linux atrás de Waitress/Nginx o header Host pode ser perdido. Sempre importe `from django.conf import settings` e utilize a variável `settings.BASE_URL` do ambiente para garantir consistência entre os ambientes.
